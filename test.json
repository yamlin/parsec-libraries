{
    "package": {
        "name": "parsec-clients",
        "repo": "gradle",
        "subject": "yamlin",
        "desc": "I was pushed completely automatically"
    },


    "version": {
        "name": "${TRAVIS_TAG}"
    },

    "files":
        [
        {"includePattern": "parsec-clients\/build\/libs\/parsec-client.jar",
         "uploadPattern": "parsec-clients/${TRAVIS_TAG}/$1"
        },
        {"includePattern": "parsec-clients\/build\/publications\/UnofficialPublication\/pom-default.xml",
         "uploadPattern": "parsec-clients/${TRAVIS_TAG}/$1"
        }
        ],
    "publish": true
}
